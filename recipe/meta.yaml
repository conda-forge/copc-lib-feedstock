{% set name = "copc-lib" %}
{% set version = "2.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/RockRobotic/copc-lib/archive/v{{ version }}.tar.gz
  sha256: 994c7080464c0d7959b4f9890b6e096c20fbc0c1275e70818de44578e8ecd618

build:
  number: 0
  skip: true  # [py<36 or (win and py>39)]

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - python                                 # [build_platform != target_platform]
    - pybind11                               # [build_platform != target_platform]
    - make
    - cmake >=3.18
  host:
    - python
    - pip
    - pybind11 >=2.6.0
    - scikit-build
    - laz-perf =3.0.0
    - mypy
  run:
    - python
    - laz-perf =3.0.0
    - numpy

test:
  imports:
    - copclib
  source_files:
    - cmake/*.cmake
    - test/CMakeLists.txt
    - test/*.cpp
    - test/*.py
    - test/data/*.h
    - example/CMakeLists.txt
    - example/*.cpp
    - example/*.py
  requires:
    - pytest
    - Catch2<3
    - {{ compiler('cxx') }}
    - make
    - cmake >=3.18

about:
  home: https://github.com/RockRobotics/copc-lib
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.md
  summary: copc-lib provides an easy-to-use interface for reading and creating Cloud Optimized Point Clouds

extra:
  recipe-maintainers:
    - CCInc
    - leo-stan
    - arknoll
